.login-confirm
  .login-confirm__header
    = render 'header'
    .nav-category
      .ul.nav-category__lists
        .li.nav-category__lists__list
          - breadcrumb :subscribed
          = breadcrumbs pretext: "",separator: " &rsaquo; ", class: "breadcrumb"
    %h1 登録終わりました
    = link_to "トップへ戻る", root_path, class: "home"
  .login-confirm__main
  .login-confirm__footer
  = render 'items/footer'